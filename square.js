var flky = new Flickity('.carousel', {
    cellAlign: 'left',
    contain: true,
    pageDots: false,
    // Disable page dots
    prevNextButtons: false
});

var flk2 = new Flickity('.xxx', {
    cellAlign: 'left',
    contain: true,
    pageDots: false,
    // // Disable page dots
    // groupCells: true,
    // groupCells: 2
});

// function pronounce() {
//   $('.flickty-button').append('<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-right"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>');
//  };
// function change (iconID){
//     document.getElementsByClassName("flickity-button-icon").className = "arrow-icon";

// }
var flk3 = new Flickity('.yyy', {
    cellAlign: 'left',
    contain: true,
    pageDots: false,
    // Disable page dots
});
var flk3 = new Flickity('.aaa', {
    cellAlign: 'left',
    contain: true,
    pageDots: false,
    // Disable page dots
});
var flk3 = new Flickity('.bbb', {
    cellAlign: 'left',
    contain: true,
    pageDots: false,
    // Disable page dots
});
// $(function() {
//     $(window).load(function() {
//         var slide = $(".models-slide")
//         var models = $(".models-text")
//         slide.addClass('models-slide')
//         models.addClass('model-text')
//     })
// })